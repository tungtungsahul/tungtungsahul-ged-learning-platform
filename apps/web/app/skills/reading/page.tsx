"use client";
import {useState} from "react";import {Shell} from "../../../components/Shell";import {Chatbot} from "../../../components/Chatbot";
export default function Reading(){
 const [selected,setSelected]=useState("");const[word,setWord]=useState("");const[hl,setHl]=useState(false);
 function lookup(){const d:Record<string,string>={"capital expenditure":"Spending on long-term assets.","counterargument":"A claim that challenges another claim.","infrastructure":"The systems and resources that support an activity."};setWord(d[selected.toLowerCase()]||"No local definition found. Ask GED AI Tutor.")}
 return <Shell><div className="topbar"><b>Reading Skill Lab</b></div><div className="content"><h1 className="title">Reading: main idea, inference, evidence</h1><div className="card" style={{marginTop:18}}><div className="toolbar"><button className="btn btn-soft" onClick={()=>setHl(!hl)}>{hl?"Highlight active":"Highlight mode"}</button><button className="btn btn-muted" onClick={()=>setSelected(window.getSelection()?.toString()||"")}>Dictionary from selection</button></div><p className="passageText" style={{marginTop:14}}>When information becomes abundant, learners still need reliable ways to evaluate it. A strong reader separates a central claim from a supporting detail, checks whether evidence actually supports the claim, and notices when a writer answers a counterargument. These skills matter in school, work, and public life.</p><div style={{display:"flex",gap:8,marginTop:14}}><input value={selected} onChange={e=>setSelected(e.target.value)} placeholder="Type a word" style={{padding:10,border:"1px solid #ddd",borderRadius:10}}/><button className="btn btn-primary" onClick={lookup}>Look up</button><span className="badge">{word||"Definition appears here"}</span></div></div><Chatbot context={{subject:"READING",skill:"Main idea, inference, evidence"}}/></div></Shell>
}
